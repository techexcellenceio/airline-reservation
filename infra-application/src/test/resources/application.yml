spring:
  jpa:
    open-in-view: false
  profiles:
    default: test-jpa-and-stub

application:
  messaging:
    broker:
      embedded: true
    host: localhost
    port: 5672
    queue:
      reserved-ticket:
        name: rabbitMqQueue